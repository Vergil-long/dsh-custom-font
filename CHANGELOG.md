# 变更记录

本项目遵循语义化版本。

## [3.0.0] - 2026-09-16

### 新增
- **插件内置字体**：自带 STIX2Text（4 个字重）与 Latin Modern Mono Light（3 个字重），
  共 7 个 WOFF 文件、约 820 KB，放在 `assets/fonts/`，由宿主端按需返回。
  **解决的是一个真问题**：这两套字体不是 Windows 自带，用户没装时浏览器会**静默回退**到别的字体——
  用户以为在用推荐排版，其实不是，界面上也看不出任何区别。现在装上插件就有效果一致，
  **且不修改用户系统**（不写注册表、不需要管理员权限）。可在高级设置里关掉。
- 一键「**应用作者推荐**」：作者为安装本插件的用户推荐的一套排版（公文/学术观感）——正文 16px 仿宋_GB2312 + STIX2Text；一级标题小标宋 22px 居中不加粗；二级黑体 18/900；三级楷体 18/900；四~六级仿宋 18/900；链接 DeepSeek 蓝 `#4D6BFE`（悬停 `#2E4FD8`）。
  > 这是一套**推荐**，不是规定：任何一项都能在设置页里改掉，改完就按用户的来。
- 版式开关（作者推荐里「不是字体」的那些要求）：正文两端对齐、一级标题居中 / 不加粗、引用块斜体 + 左侧 2px 竖线 `#a2a9b1`、行内代码 GitHub 灰底、表头深底浅字、链接自定义颜色（深色模式自动换亮蓝）。**全部收在「高级设置」里，基础页保持干净。**
- **设置持久化到 `~/.dsh/dsh-custom-font/settings.json`**：换桌面外壳不再丢设置（原来存 localStorage，按外壳应用数据目录隔离）。
- 「导出设置」/「导入设置」按钮。
- 状态行会说清"推荐字体由谁提供"：系统里有就说有；系统里没有而内置已生效，就说"已由插件内置字体接管"。

### 变更
- **链接色由传统公文红 `#a00` 改为 DeepSeek 蓝 `#4D6BFE`**（悬停 `#2E4FD8`）：红链接在深色界面里太扎眼。只迁移旧默认值那两种红，用户自己选的红色不动。
- 默认值改为作者推荐的那套（正文 18→16px、西文 Times New Roman→STIX2Text、标题逐级生效等）。用户已有的设置**优先于默认值**，升级不覆盖。
- 标题各级使用各自合适的后备字体族（一级衬线、二级无衬线、三~六级衬线），不再一律落到无衬线。
- 预览里的链接示例改为 `https://www.deepseek.com/`。

### 修复
- **"系统缺推荐字体"检测失效**：原来查的是字体查找表，而那张表预装了内置兜底字体，所以永远判定成"系统里有"。
  改为单独记录系统真正提供的字体名，缺失提示才准。
- 读 Windows 字体库时不再设置 `[Console]::OutputEncoding`：PowerShell 5.1 会因此写出 UTF-8 BOM，导致字体列表整体解析失败、下拉框只剩内置兜底列表。
- 读取系统字体失败（受限沙箱会 EPERM）时降级成「读不到字体」，不再让整条接口 500。
- 样式热重载：已存在的 `<style>` 内容变了会被替换（原来"看到已存在就返回"，插件升级后新样式永不生效）。

### 说明
- **数字跟随西文**：CSS 字体栈里西文字体排在中文之前，浏览器按"从左往右找第一个能显示该字符的字体"取值，
  所以数字与英文都用西文字体（中文字体如仿宋本身也含数字字形，顺序反了数字会悄悄变成中文样式）。
  自检对**每一条**字体规则都断言了顺序，防止以后改坏。

## [2.1.0] - 2026-09-10
- 首个公开发布版：设置页可视化改字体/字号/加粗/颜色，自动读 Windows 系统字体库，中英文分开，h1~h6 / 引用 / 行内代码 / 代码块 / 界面文字逐项可调，一键恢复默认。
